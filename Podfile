# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ReadyPlayer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ReadyPlayer
  pod 'Firebase/Auth'
  pod 'Firebase/Storage'
  pod 'Firebase/Analytics'
  pod 'Firebase/Database'

  target 'ReadyPlayerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ReadyPlayerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'ReadyPlayer WatchKit App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ReadyPlayer WatchKit App

end

target 'ReadyPlayer WatchKit Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ReadyPlayer WatchKit Extension

end
